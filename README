For the installation of the  daemon WebServer you just need to run the next command:

    bash ./scriptDeArranque.sh 

    NOTE: you will the to go to the folder where you have downloaded the daemon.

For the uninstallation do the same but with this line:

    sudo bash ./scriptDeDesinstalacion.sh